# Server deps. Install with: python -m pip install -r requirements.txt
# Use the same interpreter you use to run uvicorn (see python-dotenv note below).
python-dotenv>=1.0.0
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
